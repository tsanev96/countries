import { createMuiTheme } from "@material-ui/core";

export const rootTheme = createMuiTheme({
  palette: {
    primary: { main: "#FF0000" },
    secondary: { main: "#000" },
  },
  typography: {
    fontFamily: "Arial",
  },
});
